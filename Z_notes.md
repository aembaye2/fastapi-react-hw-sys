# cloned from:
https://github.com/Codeunia/QuizAPI-FastAPI-Backend.git
## How to start this project:
# Backend: be at the root of the project
venv/Scripts/activate
# to identify the backend venv properly use:
.\venv\Scripts\python.exe -m uvicorn app.main:app --reload

# frontend
cd frontend
# npm install (already done)
npm run dev

# admin login 
gmail; Man50@

# Git push:
git add .
git commit -m "randomized math question successfully done; python like editor on the UI created 7/10/2026"
git push
